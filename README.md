# Froof Programmatic SEO Landing Pages

A high‑conversion, programmatic SEO solution for Froof's Massachusetts roofing market. The app generates hyper‑local landing pages for every town, complete with dynamic SEO metadata, glass‑morphism design, and video testimonial support.

## ✨ Features

- **Dynamic routing** – `/locations/[region]/[town]-roofing-company` creates a static page per town.
- **Data‑driven town layer** – `src/data/ma‑towns.ts` stores region, housing stock, pain points, and optional video testimonial metadata.
- **Premium design system** – Vanilla CSS with brand colors, Inter/Outfit fonts, glass cards, and micro‑animations.
- **VideoTestimonial component** – Showcases local homeowner videos with lazy‑loaded thumbnails.
- **SEO‑ready** – JSON‑LD schema (`LocalBusiness`, `FAQPage`), dynamic meta titles/descriptions, breadcrumb markup.
- **Conversion‑focused UI** – Transparent pricing tables, financing CTA, trust‑signal badges.
- **Static generation** – All pages pre‑rendered for blazing‑fast Core Web Vitals.

## 🛠 Tech Stack

- **Framework**: Next.js 16 (App Router) with TypeScript
- **Styling**: Vanilla CSS + CSS modules (no Tailwind)
- **Images**: `next/image` with WebP assets
- **Version control**: Git + GitHub

## 📦 Getting Started

```bash
# Clone the repo
git clone https://github.com/one7L/FROOF-Programmatic-SEO-Landing-Pages.git
cd Froof-Programmatic-SEO-Landing-Pages

# Install dependencies
npm install

# Run the development server
npm run dev
```

Open `http://localhost:3000` in your browser.

## 📂 Project Structure

```
src/
├─ app/                # Next.js App Router pages
│   └─ locations/[region]/[townSlug]/page.tsx   # Dynamic town page
├─ components/         # UI components (Hero, PricingModule, …)
│   ├─ VideoTestimonial.tsx
│   └─ …
├─ data/               # Town data layer (ma‑towns.ts)
├─ styles/             # Global CSS and design tokens
└─ …
public/
├─ images/hero/       # Hero images per town
└─ …
```

## 🏘 Adding a New Town

1. Open `src/data/ma‑towns.ts` and add a new entry to the `TOWNS` object:

   ```ts
   {
     slug: "new‑town",
     name: "New Town",
     county: "County",
     region: "metro-west", // one of the defined Region values
     housingStock: "…",
     primaryPainPoint: "…",
     historic: false,
     snowBelt: false,
     coastal: false,
     heroImage: "/images/hero/new-town.png",
     videoTestimonial: {
       url: "https://example.com/video.mp4",
       thumbnail: "/images/hero/new-town.png",
       homeownerName: "John Doe",
     },
   }
   ```

2. Add a hero image to `public/images/hero/`.
3. Run `npm run build` – the new page will be generated automatically.

## 🎥 VideoTestimonial Component

The component lives in `src/components/VideoTestimonial.tsx`. It expects:

- `townName`
- `videoUrl`
- `thumbnail`
- `homeownerName`
It lazy‑loads the video and falls back to a placeholder image.

## 🚀 Deployment

The app can be deployed to Vercel, Netlify, or any Node.js host. The static build output is in `.next/`.

```bash
npm run build   # creates a production‑ready static site
npm start       # serves the built site
```

## 📜 License

MIT – feel free to fork and adapt for other markets.

---
*Generated by Antigravity – your AI‑powered coding assistant.*
